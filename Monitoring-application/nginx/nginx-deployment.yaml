apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-ms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-ms
  template:
    metadata:
      labels:
        app: nginx-ms
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
